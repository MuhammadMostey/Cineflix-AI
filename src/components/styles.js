export const customStyles = (theme) => {
  return {
    root: {
      display: "flex",
      height: "100%",
    },
    content: { padding: "2em", flexGrow: "1" },
    toolbar: { height: "70px" },
  };
};
